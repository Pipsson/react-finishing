// @flow
import  React from 'react';


function DynamicRandering() {

    const isLoading = true;
    if (isLoading) return <h1>Loading...</h1>;

    return (
        <div>

        </div>
    );
}
export default DynamicRandering;